apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pedido-app.fullname" . }}-test-connection"
  labels:
    {{- include "pedido-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pedido-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
